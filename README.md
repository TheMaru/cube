# Cube

Small experiment to start open up a window and display a rotating wire cube in
order to learn more about graphics rendering.

I hope to make it compilable on different OS System.

## building

run `make` in the main folder. This should generate an executable file named
cube in your CWD
